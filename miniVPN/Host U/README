*************************************************************************
* auth-vpntlsclient.c vpn client program via TLS with user authentication 
*************************************************************************

--------------------------
To compile the program: 
--------------------------
$ make 


--------------------------
To run the client: 
--------------------------

First make sure you have CA certificates in ca_client and server hostname is matched to IP in /etc/hosts

$ sudo ./auth-vpntlsclient [server hostname] [server port]

Then in new terminal configure routing

$ ../client-config.sh
